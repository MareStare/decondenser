{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension",
      "type": "extensionHost",
      "request": "launch",
      "args": ["--extensionDevelopmentPath=${workspaceFolder}/decondenser-vscode"],
      "outFiles": ["${workspaceFolder}/decondenser-vscode/dist/**/*.js"],
      "preLaunchTask": "${defaultBuildTask}"
    }
  ]
}
